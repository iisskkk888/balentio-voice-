# Быстрая шпаргалка по проекту Balentio Voice

**Для:** Aram (личный reference)
**Дата:** 2024-12-22
**Цель:** Краткое понимание структуры проекта и работы с Claude Code

---

## 🤖 Как Claude Code использует агентов?

### 3 способа работы агентов:

#### 1️⃣ Автоматически через команду `/research`
```
Ты пишешь: /research "Как работает транскрипция?"
    ↓
Claude САМ запускает 3 агента параллельно:
  - codebase-locator (найти файлы)
  - codebase-analyzer (понять как работает)
  - codebase-pattern-finder (найти примеры)
    ↓
Результат: Документ в specs/research/
```

**Когда использовать:** Когда изучаешь новую часть кода

#### 2️⃣ Вручную (ты просишь явно)
```
Ты пишешь: "Используй codebase-locator чтобы найти все файлы overlay"
    ↓
Claude запускает ТОЛЬКО этот агент
    ↓
Результат: Список файлов (без документа)
```

**Когда использовать:** Нужна конкретная информация быстро

#### 3️⃣ Автоматически (Claude сам решает)
```
Ты пишешь: "Создай API endpoint для транскрипции"
    ↓
Claude думает: "Это backend задача"
    ↓
Автоматически запускает backend-api-architect
    ↓
Результат: Код по паттернам проекта
```

**Когда происходит:** Просто работаешь, Claude сам всё делает

---

## 📋 Полезные команды

### `/research` — Исследование кода
```bash
/research "Как работает транскрипция?"
/research "Где используется функция X?"
/research "Покажи все API интеграции"
/research "Как сделана авторизация?"
```

**Что получишь:** Документ в `specs/research/` с полным анализом

### `/commit` — Создать коммит
```bash
/commit
```

**Что происходит:**
1. Claude анализирует изменения
2. Предлагает commit message (conventional commits)
3. Спрашивает подтверждение
4. Делает коммит

### `/describe-pr` — Описание для PR
```bash
/describe-pr
```

**Что получишь:** Готовое описание для GitHub Pull Request

---

## 🤖 Список агентов

| Агент | Для чего | Пример использования |
|-------|---------|---------------------|
| **codebase-locator** | Найти ГДЕ файлы | "Найди все файлы с overlay" |
| **codebase-analyzer** | Понять КАК работает | "Объясни как работает эта функция" |
| **codebase-pattern-finder** | Найти примеры | "Покажи примеры error handling" |
| **backend-api-architect** | Backend API | "Создай API endpoint" |
| **frontend-ui-specialist** | UI компоненты | "Создай overlay window компонент" |
| **query-layer-specialist** | TanStack Query | "Добавь mutation для создания записи" |

---

## 📁 Структура проекта

### Корневые файлы (важные):
```
CLAUDE.md                    ← Entry point для Claude Code
AGENTS.md                    ← Навигатор (ссылки на rules)
BALENTIO_VOICE_MVP.md        ← Твой план развития (PRD)
README.md                    ← Документация проекта
package.json                 ← Monorepo настройки
```

### Правила разработки:
```
rules/
├── typescript.md            ← Стиль TypeScript кода (213 строк!)
├── svelte.md                ← Паттерны Svelte (256 строк)
├── error-handling.md        ← trySync/tryAsync (149 строк)
├── git.md                   ← Conventional commits (212 строк)
├── styling.md               ← CSS/Tailwind (33 строки)
└── general-guidelines.md    ← Workflow (74 строки)
```

**Когда читать:**
- Пишешь TypeScript → `@rules/typescript.md`
- Делаешь коммит → `@rules/git.md`
- Работаешь с UI → `@rules/svelte.md`

### Главное приложение:
```
apps/whispering/
├── src/                     ← Frontend (Svelte + TypeScript)
│   ├── lib/
│   │   ├── services/        ← Бизнес-логика (платформо-независимая)
│   │   ├── query/           ← TanStack Query (реактивность)
│   │   ├── components/      ← UI компоненты
│   │   └── constants/       ← Константы
│   └── routes/              ← Страницы
├── src-tauri/               ← Desktop backend (Rust)
├── ARCHITECTURE.md          ← 🔴 ЧИТАЙ! Объясняет архитектуру
└── README.md                ← Документация приложения
```

### История фич (опционально):
```
specs/                       ← 130+ спек (история разработки)
└── 20251219T120345-feature.md
```

**Формат:** `YYYYMMDDTHHMMSS-feature-name.md`

---

## 🎯 Workflow для Balentio Voice

### 1. Изучение проекта (сейчас):
```bash
# Понять архитектуру
/research "Как работает трёхслойная архитектура (Service → Query → UI)?"

# Понять транскрипцию
/research "Как работает транскрипция от аудио до текста?"

# Понять overlay (если есть)
/research "Есть ли уже overlay window или нужно с нуля?"

# Понять auth
/research "Как сделана авторизация в проекте?"
```

### 2. Начало работы над фичей:
```
1. Открой BALENTIO_VOICE_MVP.md
2. Выбери первую задачу (например: Backend Infrastructure)
3. Создай coding-prompt.md для этой фичи
4. Работай над задачей
5. Тестируй
6. Удали coding-prompt.md
7. Следующая фича
```

### 3. Во время разработки:
```
Просто говори задачу, Claude сам использует агентов:

"Создай API endpoint для транскрипции"
→ Claude сам запустит backend-api-architect если нужно

"Создай overlay window компонент"
→ Claude сам запустит frontend-ui-specialist если нужно

"Сделай коммит"
→ /commit или просто скажи "сделай коммит"
```

---

## 🔑 Ключевые правила (из rules/)

### TypeScript:
- ✅ Всегда `type` вместо `interface`
- ✅ Co-locate типы (не создавай `types/models.ts`)
- ✅ Константы: `SCREAMING_SNAKE_CASE`

### Error Handling:
- ✅ Используй `tryAsync` для async кода
- ✅ Используй `trySync` для sync кода
- ✅ Возвращай `Ok()` для graceful handling
- ✅ Возвращай `Err()` для propagation

### Git:
- ✅ Conventional commits: `feat(scope): description`
- ✅ Lowercase после двоеточия
- ✅ Императивное наклонение ("add" не "added")
- ❌ НЕТ Claude attribution / watermarks

### Svelte:
- ✅ Используй `createMutation` для mutations
- ✅ Передавай `onSuccess/onError` в `.mutate()`
- ✅ Self-contained components (не управляй state в родителе)

---

## 📝 Примеры использования `/research`

### Пример 1: Архитектура
```
/research "Как работает транскрипция от начала до конца?"

Результат:
specs/research/2024-12-22-transcription-flow.md
  - Схема: Audio → Blob → Service → Query → UI
  - Все файлы с номерами строк
  - Примеры кода
```

### Пример 2: Паттерны
```
/research "Как в проекте делают auth?"

Результат:
specs/research/2024-12-22-auth-patterns.md
  - Где хранятся токены
  - Как проверяется авторизация
  - Примеры использования
```

### Пример 3: API интеграции
```
/research "Как интегрированы сторонние API? (OpenAI, Groq, ElevenLabs)"

Результат:
specs/research/2024-12-22-api-integrations.md
  - Структура каждого провайдера
  - Общий интерфейс
  - Как добавить нового провайдера
```

### Пример 4: Перед рефакторингом
```
/research "Где используется функция transcribeBlob?"

Результат:
specs/research/2024-12-22-transcribeBlob-usage.md
  - 12 мест где вызывается
  - Зависимости
  - Рекомендации для безопасного рефакторинга
```

---

## 🚀 Быстрый старт для первой задачи

### Задача: Backend Infrastructure (Supabase + PostgreSQL)

#### Шаг 1: Исследование
```bash
/research "Есть ли уже Supabase интеграция? Есть ли Drizzle ORM настройка?"
/research "Как организован backend в packages/epicenter?"
```

#### Шаг 2: Создание плана
```
Создай coding-prompt.md для Backend Infrastructure:
- Feature description
- Implementation plan
- Database schema
- Testing strategy
```

#### Шаг 3: Реализация
```
"Настрой Supabase проект"
"Создай database schema (users, subscriptions, usage_logs)"
"Настрой Drizzle ORM"
"Создай backend API (Hono/Elysia)"
```

#### Шаг 4: Тестирование
```
"Запусти тесты"
"Проверь миграции"
```

#### Шаг 5: Коммит
```bash
/commit
```

#### Шаг 6: Следующая задача
```
Удали coding-prompt.md
Выбери следующую фичу из BALENTIO_VOICE_MVP.md
Повтори цикл
```

---

## 💡 Советы

### Когда использовать `/research`:
- ✅ Перед началом работы над новой фичей
- ✅ Когда не понимаешь как что-то работает
- ✅ Перед большим рефакторингом
- ✅ Когда нужен документ для будущего

### Когда НЕ использовать `/research`:
- ❌ Для простых вопросов ("где файл X?")
- ❌ Во время активной разработки (просто работай)
- ❌ Когда нужен быстрый ответ (спроси напрямую)

### Как работать с правилами (rules):
- Не нужно загружать всё сразу
- Говори "используй @rules/typescript.md" когда важно
- Claude должен сам загружать когда работает в конкретной области
- Если Claude не следует правилам → напомни явно

### Как работать с агентами:
- Не нужно думать про них постоянно
- Просто говори задачу
- Claude сам решит нужен ли агент
- Используй явно только для `/research` или когда точно знаешь что нужно

---

## 🔗 Полезные ссылки

- [Aqua Voice (референс)](https://aquavoice.com/)
- [ElevenLabs API Docs](https://elevenlabs.io/docs)
- [Supabase Docs](https://supabase.com/docs)
- [Stripe Docs](https://stripe.com/docs)
- [Tauri Docs](https://tauri.app/)

---

**Последнее обновление:** 2024-12-22
**Статус:** Проект в стадии планирования MVP


# Детальная структура проекта Balentio Voice

Этот документ объясняет, что находится в каждой папке проекта простым языком, без предположений о ваших знаниях.

---

## 📁 Корневой уровень

### `apps/` - Папка с приложениями

**Что это:** Папка, где хранятся все отдельные приложения проекта. Каждое приложение - это самостоятельная программа или сайт.

**Что внутри:**
- `whispering/` - Главное desktop приложение (см. детали ниже)
- `landing/` - Маркетинговый сайт (см. детали ниже)
- `demo-mcp/` - Демо-пример работы с базой данных (см. детали ниже)
- `posthog-reverse-proxy/` - Техническая утилита для аналитики (см. детали ниже)

**Для Balentio Voice:** Очень важная папка! Здесь лежит главное приложение, которое вы будете переделывать.

---

### `packages/` - Папка с общим кодом

**Что это:** Библиотеки общего кода, которые используются несколькими приложениями. Это как строительные блоки, из которых собираются приложения.

**Аналогия:** Представьте, что приложения - это дома, а packages - это кирпичи, окна, двери, которые можно использовать в разных домах.

**Что внутри:**
- `epicenter/` - Основная бизнес-логика приложения (см. детали ниже)
- `ui/` - Готовые визуальные компоненты (кнопки, окна, формы) (см. детали ниже)
- `constants/` - Постоянные значения (список языков, настроек и т.д.)
- `shared/` - Общие утилиты и функции
- `config/` - Конфигурационные файлы для инструментов разработки
- `svelte-utils/` - Вспомогательные функции для Svelte (технология интерфейса)
- `vault-core/` - Код для работы с "хранилищем" данных

**Для Balentio Voice:** Очень важная папка! В `epicenter/` находится основная логика, которую нужно будет адаптировать. В `ui/` находятся готовые компоненты интерфейса.

---

### `rules/` - Правила написания кода для Claude Code

**Что это:** Набор инструкций, которые объясняют Claude Code (искусственному интеллекту), как правильно писать код для этого проекта.

**Аналогия:** Это как "учебник по стилю письма" для робота-программиста. В нем написано: "пиши так, не пиши эдак".

**Что внутри:**
- `typescript.md` - Правила написания TypeScript кода
- `svelte.md` - Правила написания Svelte компонентов (визуальных частей)
- `error-handling.md` - Как правильно обрабатывать ошибки
- `git.md` - Как правильно делать коммиты (сохранения) в Git
- `github.md` - Как отвечать на вопросы пользователей в GitHub
- `documentation.md` - Как писать документацию
- `social-media.md` - Как писать посты в соцсетях
- `styling.md` - Правила оформления внешнего вида
- `rust.md` - Правила написания Rust кода (для desktop части)
- `posthog.md` - Правила настройки аналитики
- `general-guidelines.md` - Общие правила разработки
- `README.md` - Описание того, как работает система правил

**Для Balentio Voice:** Можно оставить как есть. Эти правила помогут Claude Code писать код в едином стиле.

---

### `specs/` - Спецификации задач

**Что это:** Папка с детальными планами и техническими заданиями для разработки новых функций.

**Аналогия:** Это как чертежи для строительства - детальные планы того, что нужно сделать.

**Формат имен файлов:** `20250105T155230-название-задачи.md`
- `20250105T155230` - дата и время создания (2025 год, 05 января, 15:52:30)
- `название-задачи` - краткое описание задачи

**Примеры файлов:**
- `20250107T000000-infinite-toast-duration.md` - Как добавить бесконечную длительность уведомлений
- `20250108T230000-ui-monorepo-refactor.md` - План рефакторинга UI компонентов
- `20250120T012752-ui-overhaul.md` - План переделки интерфейса

**Что внутри каждого файла:**
- Описание проблемы
- План решения
- Технические детали
- Что нужно изменить

**Для Balentio Voice:** Эти файлы относятся к старому проекту (Whispering). Можно не трогать - они не мешают. Для новых задач создавайте новые spec-файлы.

---

### `docs/` - Документация и справочные материалы

**Что это:** Папка с документами для людей (не для Claude Code). Здесь хранятся статьи, руководства, заметки о дизайне.

**Разница между `specs/` и `docs/`:**
- `specs/` = Планы БУДУЩЕЙ работы ("что нужно сделать")
- `docs/` = Описание СУЩЕСТВУЮЩЕГО кода ("как это работает")

**Что внутри:**
- `architecture/` - Документы об архитектуре проекта (как устроена система)
- `articles/` - Статьи о проекте
- `blog/` - Черновики постов для блога
- `guides/` - Руководства для разработчиков
- `launches/` - Заметки о запусках новых версий
- `patterns/` - Паттерны проектирования (типовые решения)
- `release-notes/` - Заметки о выпущенных версиях
- `specs/` - Старые спецификации (не путать с корневой папкой `specs/`)

**Для Balentio Voice:** Можно оставить для справки, особенно `architecture/`. Поможет понять, как устроен проект.

---

### `.claude/` - Настройки Claude Code

**Что это:** Папка с настройками для Claude Code (искусственного интеллекта, который помогает писать код).

**Что внутри:**
- `commands/` - Пользовательские команды (например, `/commit`, `/research`)
- `agents/` - Настройки агентов (специализированных помощников)
- `settings.local.json` - Локальные настройки Claude Code (не попадают в Git)

**Для Balentio Voice:** Не трогайте эту папку. Она нужна для работы Claude Code.

---

### `examples/` - Примеры использования

**Что это:** Папка с примерами кода и демонстрациями возможностей проекта.

**Что внутри:**
- `basic-workspace/` - Базовый пример рабочего пространства
- `content-hub/` - Пример работы с контентом
- `stress-test/` - Пример нагрузочного тестирования

**Для Balentio Voice:** Скорее всего не понадобится. Это примеры для разработчиков, которые хотят использовать код проекта.

---

### `.github/` - Настройки GitHub

**Что это:** Папка с настройками для GitHub (платформы, где хранится код).

**Что внутри:**
- `workflows/` - Автоматические действия (например, автоматическая сборка при каждом коммите)
- `ISSUE_TEMPLATE/` - Шаблоны для создания задач (issues) на GitHub

**Для Balentio Voice:** Можно удалить или изменить workflows, если не планируете использовать автоматизацию GitHub.

---

### `scripts/` - Вспомогательные скрипты

**Что это:** Папка с утилитами и скриптами для разработки.

**Для Balentio Voice:** Посмотрите, что там есть. Возможно, какие-то скрипты пригодятся.

---

### `logos/` - Логотипы проекта

**Что это:** Папка с изображениями логотипов в разных форматах и размерах.

**Для Balentio Voice:** Замените на логотипы Balentio Voice.

---

### `.vscode/` - Настройки VS Code

**Что это:** Папка с настройками редактора кода Visual Studio Code.

**Для Balentio Voice:** Можно оставить как есть, если используете VS Code.

---

### `.git/` - История Git

**Что это:** Служебная папка Git. Хранит всю историю изменений кода.

**Для Balentio Voice:** НЕ ТРОГАЙТЕ эту папку! Она управляется автоматически.

---

### Корневые файлы

#### `package.json`
**Что это:** Главный файл конфигурации проекта. Определяет зависимости (какие библиотеки использует проект) и скрипты (команды для запуска, сборки и т.д.).

**Для Balentio Voice:** Нужно будет изменить название проекта с "epicenter" на что-то связанное с Balentio.

#### `CLAUDE.md` и `AGENTS.md`
**Что это:** Файлы-навигаторы для Claude Code. Объясняют, какие правила использовать и когда.

**Для Balentio Voice:** Можно оставить как есть.

#### `README.md`
**Что это:** Главное описание проекта. Это первое, что видят люди, когда открывают проект на GitHub.

**Для Balentio Voice:** ОБЯЗАТЕЛЬНО переписать! Здесь должно быть описание Balentio Voice, а не Whispering.

#### `turbo.json`
**Что это:** Конфигурация Turborepo (инструмент для управления monorepo).

**Что такое monorepo:** Один репозиторий (хранилище кода), который содержит несколько проектов (apps) и общий код (packages). Turborepo помогает эффективно собирать и управлять всеми этими частями.

**Для Balentio Voice:** Не трогайте, пока не понадобится менять процесс сборки.

#### `tsconfig.json`
**Что это:** Конфигурация TypeScript (языка программирования проекта).

**Для Balentio Voice:** Не трогайте.

#### `biome.json`
**Что это:** Конфигурация Biome (инструмент для проверки качества кода и форматирования).

**Для Balentio Voice:** Не трогайте.

---

## 📱 Детали: apps/whispering/

**Что это:** Главное desktop приложение. Это ТО САМОЕ приложение, которое вы будете переделывать в Balentio Voice.

**Технологии:**
- **Tauri** - фреймворк для создания desktop приложений с использованием веб-технологий (HTML, CSS, JavaScript)
- **Svelte 5** - современная библиотека для создания пользовательских интерфейсов
- **TypeScript** - язык программирования (JavaScript с типами)
- **Rust** - язык для написания backend части (находится в `src-tauri/`)

### Структура whispering/:

#### `src/` - Исходный код интерфейса (TypeScript + Svelte)

**Что внутри:**
- `routes/` - Страницы приложения (например, главная страница, страница настроек)
  - В Svelte используется "файловая маршрутизация": файл `routes/settings/+page.svelte` = страница `/settings`
- `lib/` - Библиотека компонентов и логики
  - `components/` - Визуальные компоненты (кнопки, формы, диалоги)
  - `services/` - Сервисы (бизнес-логика)
  - `query/` - Работа с запросами и кэшированием данных
  - `stores/` - Глобальное состояние приложения
  - И другие подпапки

**Важно:** Основная работа по переделке будет происходить именно здесь!

#### `src-tauri/` - Исходный код backend части (Rust)

**Что это:** Backend часть desktop приложения. Написана на Rust (быстрый и безопасный язык программирования).

**Что здесь происходит:**
- Работа с системой (файлы, clipboard, горячие клавиши)
- Запись аудио с микрофона
- Локальная транскрипция (если используется local модель)
- Симуляция нажатий клавиш (для вставки текста)

**Что внутри:**
- `src/` - исходный код на Rust
  - `lib.rs` - главный файл
  - `command.rs` - команды, которые можно вызвать из TypeScript
  - `recorder/` - код для записи аудио
  - `transcription/` - код для транскрипции
  - И другие файлы
- `Cargo.toml` - файл зависимостей для Rust (аналог package.json)
- `tauri.conf.json` - конфигурация Tauri приложения

**Для Balentio Voice:** Возможно, понадобится добавить код для overlay окна (плавающего окна с визуализацией).

#### `static/` - Статические файлы

**Что это:** Файлы, которые не изменяются (иконки, шрифты, изображения).

**Для Balentio Voice:** Замените иконки на иконки Balentio Voice.

#### `specs/` - Локальные спецификации

**Что это:** Спецификации, специфичные для приложения Whispering.

**Для Balentio Voice:** Можно не трогать или удалить.

#### Конфигурационные файлы:

- `package.json` - зависимости и скрипты для frontend части
- `svelte.config.js` - конфигурация Svelte
- `vite.config.ts` - конфигурация Vite (инструмент сборки)
- `tsconfig.json` - конфигурация TypeScript
- `wrangler.jsonc` - конфигурация Cloudflare Workers (вероятно, для веб-версии)

**Для Balentio Voice:** Большинство конфигов можно не трогать. Главное - изменить название проекта в package.json.

---

## 🌐 Детали: apps/landing/

**Что это:** Маркетинговый веб-сайт для привлечения пользователей.

**Технология:**
- **Astro** - современный фреймворк для создания статических сайтов (быстрых и SEO-оптимизированных)
- **Статический сайт** - сайт, который генерируется один раз и не требует сервера с базой данных

**Что внутри:**
- `src/` - исходный код сайта
  - `pages/` - страницы сайта (index.astro = главная страница)
  - `components/` - компоненты страниц (шапка, футер, секции)
  - `layouts/` - шаблоны страниц
- `public/` - статические файлы (изображения, favicon)

**Для Balentio Voice:** Нужно полностью переделать под бренд Balentio Voice:
- Изменить тексты
- Заменить скриншоты приложения
- Добавить информацию о подписке и ценах
- Изменить дизайн под новый бренд

---

## 🔌 Детали: apps/demo-mcp/

**Что это:** Демонстрационный пример работы с MCP.

**Что такое MCP:**
- **MCP** = Model Context Protocol (Протокол Контекста Модели)
- Это протокол для работы AI агентов с данными
- Позволяет AI (например, Claude Code) читать данные из разных источников (базы данных, файлы, API)

**Что делает этот пример:**
- Импортирует данные из Reddit в локальную базу данных
- Запускает MCP сервер, который предоставляет доступ к этим данным

**Для Balentio Voice:** Скорее всего не нужно. Это просто демо-пример для разработчиков.

---

## 🔄 Детали: apps/posthog-reverse-proxy/

**Что это:** Reverse proxy для PostHog аналитики.

**Что такое:**
- **PostHog** - инструмент аналитики (как Google Analytics, но open-source)
- **Reverse proxy** - "посредник", который перенаправляет запросы

**Зачем это нужно:**
- Обходит блокировщики рекламы (они блокируют прямые запросы к PostHog)
- Запросы идут через ваш домен, а не через posthog.com

**Технология:**
- **Cloudflare Workers** - serverless функции (код, который запускается на серверах Cloudflare)

**Для Balentio Voice:** Если планируете использовать PostHog аналитику - оставьте. Если нет - можно удалить.

---

## 📦 Детали: packages/epicenter/

**Что это:** Основная бизнес-логика приложения. Самый важный package!

**Почему "epicenter":** Это название проекта. Приложение Whispering - это часть большого проекта Epicenter.

**Что внутри (`src/` папка):**

### `core/` - Ядро приложения

**Что здесь:** Основные концепции и функции приложения:
- Работа с записями (recordings)
- Работа с сессиями (sessions)
- Трансформации текста (transformations)
- Настройки (settings)
- Рабочие пространства (workspaces)

### `providers/` - Провайдеры транскрипции

**Что здесь:** Код для работы с разными сервисами транскрипции:
- OpenAI Whisper API
- Groq
- Deepgram
- AssemblyAI
- Local Whisper (локальная модель)
- И другие

**Для Balentio Voice:** ЗДЕСЬ НУЖНА ОСНОВНАЯ РАБОТА!
- Удалить всех провайдеров, кроме одного (например, Deepgram или создать интеграцию с ElevenLabs)
- Упростить логику выбора провайдера

### `cli/` - Command Line Interface

**Что здесь:** Код для использования приложения через командную строку (терминал).

**Для Balentio Voice:** Вероятно не нужно, если планируете только GUI приложение.

### `indexes/` - Индексы для поиска

**Что здесь:** Код для полнотекстового поиска по записям.

**Для Balentio Voice:** Оставьте, если хотите функцию поиска по транскрипциям.

### `server/` - Серверная часть

**Что здесь:** API сервер (вероятно, для синхронизации между устройствами или для будущих веб-функций).

**Для Balentio Voice:** Это может быть полезно для вашего backend! Изучите, что здесь есть.

### Файлы экспорта:
- `index.shared.ts` - общий код для всех платформ
- `index.node.ts` - код только для Node.js (сервер, CLI)
- `index.browser.ts` - код только для браузера

**Архитектурный паттерн:** Код разделен по платформам для максимального переиспользования.

---

## 🎨 Детали: packages/ui/

**Что это:** Библиотека готовых UI компонентов (визуальных элементов).

**Технология:**
- **shadcn-svelte** - коллекция готовых компонентов для Svelte
- **Tailwind CSS** - утилитарный CSS фреймворк

**Что внутри:** 50+ папок с компонентами:
- `button/` - кнопки
- `dialog/` - модальные окна (диалоги)
- `input/` - поля ввода
- `card/` - карточки
- `toast/` - всплывающие уведомления
- `drawer/` - выдвижные панели
- `select/` - выпадающие списки
- И многие другие

**Каждая папка содержит:**
- Компонент в формате `.svelte`
- `index.ts` - файл экспорта

**Для Balentio Voice:** Очень полезная библиотека! Используйте эти компоненты для создания интерфейса. Возможно, нужно будет изменить стили под ваш бренд.

---

## 🔢 Детали: packages/constants/

**Что это:** Папка с постоянными значениями (константами), которые используются в разных частях приложения.

**Примеры констант:**
- Список поддерживаемых языков
- Список моделей транскрипции
- Настройки по умолчанию
- Форматы файлов

**Для Balentio Voice:** Нужно будет изменить некоторые константы (например, список провайдеров, список моделей).

---

## 🛠️ Детали: packages/shared/

**Что это:** Общие утилиты и функции, которые используются везде.

**Примеры:**
- Функции для работы со строками
- Функции для работы с датами
- Валидаторы
- Хелперы

**Для Balentio Voice:** Скорее всего можно оставить как есть. Это полезные утилиты.

---

## ⚙️ Детали: packages/config/

**Что это:** Общие конфигурационные файлы для инструментов разработки.

**Что внутри:**
- Конфигурация TypeScript
- Конфигурация ESLint (проверка качества кода)
- Конфигурация Prettier (форматирование кода)
- И другие

**Для Balentio Voice:** Не трогайте. Это настройки инструментов разработки.

---

## 🎭 Детали: packages/svelte-utils/

**Что это:** Вспомогательные функции для работы со Svelte.

**Примеры:**
- Хуки (hooks) для работы с состоянием
- Утилиты для анимаций
- Функции для работы с lifecycle

**Для Balentio Voice:** Можно оставить как есть.

---

## 🗄️ Детали: packages/vault-core/

**Что это:** Код для работы с "хранилищем" (vault) данных.

**Что такое vault:**
- Безопасное хранилище данных пользователя
- Локальная база данных
- Возможно, шифрование данных

**Для Balentio Voice:** Нужно изучить, как это работает. Возможно, пригодится для хранения записей пользователя.

---

## 🎯 Резюме: Что важно для Balentio Voice

### ✅ ОБЯЗАТЕЛЬНО изучить и переделать:

1. **apps/whispering/** - Главное приложение
   - `src/routes/` - Страницы приложения
   - `src/lib/components/` - Компоненты интерфейса
   - `src-tauri/` - Backend часть (для overlay окна)

2. **packages/epicenter/src/providers/** - Провайдеры транскрипции
   - Удалить лишних провайдеров
   - Оставить только нужного (или добавить ElevenLabs)

3. **apps/landing/** - Маркетинговый сайт
   - Переписать тексты
   - Изменить дизайн
   - Добавить информацию о подписке

4. **Корневой README.md** - Описание проекта
   - Переписать полностью

### 🔧 Понадобится изменить:

5. **packages/constants/** - Константы
   - Список провайдеров
   - Список моделей
   - Настройки по умолчанию

6. **packages/epicenter/src/core/** - Бизнес-логика
   - Возможно, упростить трансформации
   - Адаптировать под подписочную модель

7. **package.json файлы** - Названия проектов
   - Изменить "epicenter" и "whispering" на "balentio-voice"

### ✨ Можно оставить как есть:

8. **packages/ui/** - UI компоненты
   - Готовые компоненты можно использовать
   - Возможно, изменить цветовую схему

9. **packages/shared/** - Общие утилиты
   - Полезные функции

10. **rules/** - Правила для Claude Code
    - Помогут писать качественный код

### ❌ Можно удалить:

11. **apps/demo-mcp/** - Демо MCP
    - Не нужно для продукта

12. **examples/** - Примеры
    - Не нужно для продукта

13. **apps/posthog-reverse-proxy/** - Reverse proxy
    - Только если не используете PostHog

14. **specs/** (корневая папка) - Старые спецификации
    - Относятся к Whispering, не к Balentio Voice
    - Но не удаляйте - они не мешают!

15. **docs/** - Частично
    - Оставьте `architecture/` для справки
    - Остальное можно удалить или игнорировать

---

## 📝 Термины и концепции

### Что такое Monorepo?
**Monorepo** (моно-репозиторий) - это один git репозиторий, который содержит несколько проектов и пакетов.

**Преимущества:**
- Общий код легко переиспользовать
- Изменения в общем коде сразу видны во всех проектах
- Проще управлять версиями

**В этом проекте:**
- `apps/` = отдельные приложения
- `packages/` = общий код

### Что такое Tauri?
**Tauri** - это фреймворк для создания desktop приложений с использованием веб-технологий (HTML, CSS, JavaScript).

**Как это работает:**
- Frontend (интерфейс) = веб-технологии (HTML, CSS, JavaScript)
- Backend (системные операции) = Rust (быстрый язык)

**Преимущества:**
- Маленький размер приложения
- Высокая производительность
- Доступ к системным функциям (файлы, clipboard, горячие клавиши)

### Что такое Svelte?
**Svelte** - это современная библиотека для создания пользовательских интерфейсов.

**Особенности:**
- Компилируется в чистый JavaScript (без runtime библиотеки)
- Простой и понятный синтаксис
- Высокая производительность

**Файлы:** Имеют расширение `.svelte`

### Что такое TypeScript?
**TypeScript** - это JavaScript с типами.

**Зачем типы:**
- Ловят ошибки на этапе написания кода
- Автодополнение в редакторе работает лучше
- Код легче понимать

**Файлы:** Имеют расширение `.ts` или `.tsx`

### Что такое Rust?
**Rust** - это системный язык программирования (как C++, но более безопасный).

**Где используется в проекте:**
- Backend часть Tauri приложения (`src-tauri/`)
- Работа с системой (файлы, аудио, clipboard)

**Файлы:** Имеют расширение `.rs`

### Что такое Astro?
**Astro** - это фреймворк для создания статических сайтов.

**Особенности:**
- Генерирует HTML один раз (статический сайт)
- Очень быстрый
- Хорош для SEO (поисковая оптимизация)

**Где используется:** `apps/landing/` (маркетинговый сайт)

### Что такое Bun?
**Bun** - это современная альтернатива npm (менеджер пакетов для JavaScript).

**Преимущества:**
- Намного быстрее npm
- Совместим с npm пакетами

### Что такое Turborepo?
**Turborepo** - это инструмент для управления monorepo.

**Что делает:**
- Ускоряет сборку (кэширует результаты)
- Запускает задачи параллельно
- Собирает только то, что изменилось

---

## 🚀 Следующие шаги

1. **Прочитайте** `apps/whispering/ARCHITECTURE.md` - понять, как устроено приложение
2. **Изучите** `packages/epicenter/src/providers/` - понять, как работают провайдеры
3. **Запустите** проект локально:
   ```bash
   cd /home/aram/blnt/balentio-voice-
   bun install  # установить зависимости
   cd apps/whispering
   bun run dev  # запустить в режиме разработки
   ```
4. **Используйте** `/research` команду Claude Code для изучения кода:
   ```
   /research Какие провайдеры транскрипции используются и где они определены?
   /research Как работает система трансформаций текста?
   /research Где находятся настройки приложения?
   ```

---

## 💡 Советы

1. **Не удаляйте код сразу** - сначала изучите, что он делает
2. **Используйте /research** - Claude Code поможет понять код
3. **Читайте ARCHITECTURE.md** - там объясняется архитектура
4. **Задавайте вопросы** - лучше спросить, чем сделать неправильно
5. **Делайте коммиты часто** - чтобы можно было откатить изменения

---

*Этот документ создан для вашего личного использования. Обновляйте его по мере изучения проекта!*
